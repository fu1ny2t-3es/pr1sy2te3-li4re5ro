on:
  workflow_dispatch:
  schedule:
    - cron: '45 * */7 * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/pr1sy2te3-li4re5ro@cron
        with:
          token: ${{ secrets.token }}
